## Branch Rules
- `main` = production (protected)
- `develop` = integration
- `feature/*` = your work

## Workflow
1. Pull latest `develop`
2. Create `feature/your-task`
3. Push and open PR to `develop`
4. Wait for review approval before merging